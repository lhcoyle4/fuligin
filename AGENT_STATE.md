# FULIGIN Agent State
<!-- Updated by agents before and after every task. See COWORKING_PROTOCOL.md. -->

## Agent: Claude (Anthropic)
- **Status**: Idle
- **Branch**: agent/claude
- **Active Task**: None
- **Locked Files**: None
- **Last Updated**: 2026-05-26

## Agent: Gemini (Google)
- **Status**: Idle
- **Branch**: main
- **Active Task**: None
- **Locked Files**: None
- **Last Updated**: 2026-05-25

## Agent: Human (Louie)
- **Status**: Idle
- **Branch**: main
- **Active Task**: None
- **Locked Files**: None
- **Last Updated**: 2026-05-25

## Completed Work Log
| Agent  | Task | Commit(s) | Date |
|--------|------|-----------|------|
| Claude | Phase 1: Project rename, SDL2 setup, core game loop | (merged) | 2025 |
| Claude | Phase 2: HUD overhaul, vector font | (merged) | 2025 |
| Claude | Phase 3: World Builder, enemy spawning, AI stub | (merged) | 2025 |
| Gemini | todo.md: 14-feature roadmap | (merged) | 2025 |
| Claude | Merge all branches → main (PR #1, all features preserved) | de0c317 | 2026-05-25 |
| Claude | Mouse button keybinds: LMB=shoot, RMB=thrust, MMB=hyperspace | c9d99e5 | 2026-05-25 |
| Claude | Add FULIGIN_ACTION_LIST.md (32-item roadmap) | 226e3bb | 2026-05-25 |
| Claude | Zone Transition Banners: fade-in/out zone name overlay with zone-color tinting | agent/claude | 2026-05-25 |
| Claude | Proximity Danger Alert: pulsing CINNABAR DANGER text when UFO within 520u | agent/claude | 2026-05-25 |
| Claude | Cacogen UFO Auditory Signature: FM dual-oscillator wobble+shimmer loop | agent/claude | 2026-05-25 |
| Claude | Vector Stress-Cracking: wireframe crack lines on hit metal/crystal asteroids | agent/claude | 2026-05-25 |
| Claude | Chronicle Chord Harmonics: pentatonic FM arpeggio on rapid orb collection | agent/claude | 2026-05-25 |
| Claude | Score/Event Floats: EventFloat pool, typed HIT/CRIT/CHRONICLE/VENT labels with scale+fade | agent/claude | 2026-05-25 |
| Claude | Warp-Drive Singularity Exit FX: CRT bloom flash + 3 expanding cyan vector rings on warp arrival (default variant) | agent/claude | 2026-05-25 |
| Claude | Screen-Edge Zone Vignette: PHOTIC RUST tint scaling with zone depth, CINNABAR 2Hz beat pulse | agent/claude | 2026-05-25 |
| Claude | Gravity Slingshot: tangential boost in rift close orbit (150-380u), GRAVITY ASSIST float | agent/claude | 2026-05-25 |
| Claude | Mark items 3 (segmented bars), 4 (phosphor decay), 18 (slingshot) as done in action list | agent/claude | 2026-05-25 |
| Claude | Fuel Clock: passive reactor drain + Emergency Drift Mode (thrust block, hull breach at 10s) | agent/claude | 2026-05-25 |
| Claude | Asteroid Bowling Combos: bowling_timer fragments chain into rocks, STRIKE! float + score multiplier per chain depth | agent/claude | 2026-05-25 |
| Claude | UI Overhaul Foundation: ui_panel_terminal() + ui_panel_menu() primitives; all 12 ui_panel() calls across menus/overlays/minimap replaced with terminal variant | agent/claude | 2026-05-25 |
| Claude | HUD panel geometry (item 2): all 3 render_hud() panels converted to ui_panel_terminal(); fix duplicate-symbol linker error (state.c/game.c extern split) | agent/claude | 2026-05-25 |
| Claude | Cugel-9 board computer (item 26): committed concurrent-agent inline cugel9_say() implementation (6 narrative call sites); parallel external module discarded to avoid dual-render | agent/claude | 2026-05-25 |
| Claude | Powerup window bug fix: replaced static upgrade_options/selected_option duplicates in game.c with narrow externs sharing state.c globals; cursor now advances + Enter selects correctly | a199055 | 2026-05-25 |
| Claude | New unintegrated modules ready for follow-up: src/enemy_rustweaver.[ch] (Item 23) + src/drone_chatter.[ch] (Item 27) | a199055 | 2026-05-25 |
| Claude | FULIGIN_REFACTOR_GUIDE.md: project-local refactoring playbook synthesizing Wikipedia + Augment + FULIGIN-specific traps | 99bf0bb | 2026-05-25 |
| Claude | World-scale 10x: FUEL_REGEN_RADIUS, ZONE_HOME/INNER/VOID_RADIUS, and warp_locs[] coordinates multiplied by 10 — navigable universe is now 10x larger, encounter density near player unchanged | agent/claude | 2026-05-25 |
| Claude | Splash centering + minimap visibility + state.h syntax fix: boot text now centered on screen via vf_draw_string_centered, minimap uses ui_panel_terminal + HUD_BORDER_ACTIVE for visibility, state.h garbage lines 222-223 deleted (was blocking whole project build) | agent/claude | 2026-05-25 |
| Claude | CRT glass curvature (item 31) [IN PROGRESS]: render_crt_glass() static fn in game.c (14 edge strips + 40 corner glare diagonals, SDL2 draw calls); vg_apply_crt_glass() removed; settings_crt_curve toggle live | agent/claude | 2026-05-25 |
| Claude | Pet shield-drone chatter (Item 27) integrated: drone_chatter.c added to Makefile SRC; NpcEntity gained chatter_timer field; NPC update loop emits TARGET_UFO / TARGET_LARGE / HELP / COORDINATING events based on proximity & game state; drone_chatter_update() ticks each frame; drone_chatter_render() called after NPC shapes; full compile-clean | agent/claude | 2026-05-26 |
| Claude | Rust-Weaver Drones (Item 23) integrated + Makefile SRC repair: enemy_rustweaver.[ch] wired into game.c (init, update, render, Zone 2+ spawn pacing 35-55s with jitter, player-bolt-vs-drone hit test, corrosive-spit-vs-player hull damage that bypasses Ether Shroud and Phase Shift). rustweaver_render signature gained camera_x/y params so the raw SDL_RenderDrawLineF calls render in screen-space. Makefile SRC line restored to include the previously-dropped src/state.c and src/game_data.c (link was broken — ~60 undefined references); also added src/enemy_rustweaver.c. Clean build, exe produced (314KB), no new warnings. | agent/claude | 2026-05-26 |
| Claude | Ascians (Item 21) integrated: new module enemy_ascian.[ch] — voiceless polygon-patrol interceptors. Each Ascian walks the perimeter of a regular N-gon (3-6 sides) at constant angular speed, no pursuit, no randomness. Detect radius 600u; on cooldown elapses, fires a tight three-bolt magenta wedge (0.18 rad spread) at the player. Wired into game.c: ascian_init in game_init, ascian_update after rustweaver_update in tick, ascian_render after rustweaver_render. Zone 3+ spawn pacing (55-85s timer, 800-1100u ring); cycles triangle/square/pentagon patrol shapes deterministically. Collision section 5c handles player-bolt vs Ascian (+150 score, ASCIAN DOWN float) and Ascian-bolt vs player (standard hit, Void Stone soak, INTERCEPTED float + Cugel-9 quip). Makefile SRC updated. Full project rebuilds clean, 319KB exe. | agent/claude | 2026-05-26 |
| Claude | Lictors (Item 22) integrated: new module enemy_lictor.[ch] — elite aggressive interceptor saucers that pursue the player at high speed. Pursuit AI: per-frame thrust vector toward player (240 u/s²) with 260 u/s speed cap and 0.18/s linear drag — predator arc, not a perfect tracker. Aimed bolt every 2.4s when player within 900u detect radius (340 u/s amber streak). Wired into game.c: lictor_init in game_init, lictor_update after ascian_update, lictor_render after ascian_render. Zone 4+ spawn pacing (90-130s timer, 900-1200u ring) with Cugel-9 foreshadow quip on arrival. Collision section 5d handles player-bolt vs Lictor (+200 score, LICTOR DOWN float) and Lictor-bolt vs player (standard hit, Void Stone soak, PURSUED float + Cugel-9 quip). "Arrival accelerates beat" implemented via lictor_alive_count() accessor — global beat-pulse code compresses tempo by ~35% (clamped to 0.15s minimum) while any Lictor is alive. Cinnabar narrow-delta silhouette + amber bolt streaks, visually distinct from Ascian magenta and Rust-Weaver acid-green. Makefile SRC updated. Clean build, 325KB exe, no new warnings. | agent/claude | 2026-05-26 |
| Claude | Item 13 (Five-Zone Scaffolding): added DEEP DRIFT as zone 4. New `ZONE_ABYSS_RADIUS` (14000u) in entities.h. `get_zone()` extended to return 0-4. All 6 zone-name arrays (`tl_zone_names`/`br_zone_names`/`mm_zn` in both game.c and ui_hud.c) and the `zone_banner_names` array extended to 5 entries with "DEEP DRIFT" / ">>> DEEP DRIFT <<<". `HUD_ZONE_ACCENT[]` and `UI_ZONE_COLORS[]` extended to 5 entries with a bleached bone-white accent (220,215,190) for DEEP DRIFT; `ui.h` extern declarations updated to match. `ui_zone_color()` clamp raised to 4. Beat-pulse vignette divisor in game.c line 6515 retargeted 3.0f → 4.0f so the zone_f 0→1 ramp now spans 5 zones. **Activates dormant Lictor spawn path** (Item 22 spawn was gated on `player_zone >= 4`, which `get_zone()` could never return — now fires at distances > 14000u from origin). Not built/pushed from this session (sandbox has no working mingw toolchain and no git push access); changes staged locally for human review/build. | (staged) | 2026-05-26 |
| Claude | Solar Flare Cycles (Item 33) + Star-Shadow Radiation Shielding (Item 34) shipped: completed and committed the pending Solar Flare implementation (3-phase COUNTUP/WARNING/ACTIVE state machine, fuel-drain ×3 hook, cugel9 telegraph, FLARE INBOUND/SOLAR FLARE/FLARE PASSED event floats, Home Space exempt). Added Star-Shadow mechanic on top: per-frame shadow scan walks asteroid pool during ACTIVE phase, accepts any large/medium asteroid lying in a corridor from player → origin (half-width `SOLAR_FLARE_SHADOW_HALF_W = 80u + 0.6 × asteroid.radius`); when shielded, drain multiplier drops from 3.0× to `SOLAR_FLARE_SHADOW_MULT = 1.4×`. Visual: low-alpha cinnabar tether line drawn from each shielding asteroid's player-facing edge out to the player, alpha pulse-modulated at 8 rad/s and brightness-scaled by how centred the player sits in the corridor. Verified clean build via Makefile from worktree: 327KB launch_fuligin.exe, no new warnings. Tracker hygiene: ticked Lore §3 in todo.md, appended Items 33+34 to FULIGIN_ACTION_LIST.md. **Note:** prior-session worktree's claim that Items 24/25 (Scavenger Probes / EMP Sentinels) are integrated is stale — `src/enemy_scavenger.c` and `src/enemy_emp_sentinel.c` do not exist on the agent/claude branch and game.c contains no references; the action-list shows them unchecked, which matches actual code state. | agent/claude | 2026-05-26 |
| Claude | EMP Sentinels (Item 25) integrated: new module `src/enemy_emp_sentinel.[ch]` copied into worktree and wired into `game.c`. `emp_sentinel_init()` in `game_init()`; `emp_sentinel_update(dt, player.pos.x, player.pos.y)` after `lictor_update()` in tick; `emp_sentinel_render(g_renderer, camera_pos.x, camera_pos.y)` after `lictor_render()` in `render_entities()`. State machine IDLE→CHARGE(1.4s telegraph)→PULSE(0.45s, ring expands to 380u)→COOLDOWN(4s). Zone 2+ spawn pacing 50-80s with jitter, 750-1000u ring; pool cap 3. Collision section 5e: player-bolt vs sentinel = single-bullet kill, +100 score, +6 XP orb, "SENTINEL DOWN" float; pulse-ring vs player = sets `player.emp_lock_timer = 1.6s` (re-armed each frame inside the ring), "EMP LOCK" float, sad Cugel-9 quip. New field `player.emp_lock_timer` added to BOTH `include/entities.h` ShipEntity AND the inline ShipEntity duplicate in `src/game.c` line 253 (the inline copy is what game.c actually uses — discovered during build). Input suppression in `update_player_physics()`: captures `snap_emp_angle` pre-input, after input phase if `emp_lock_timer > 0` snaps `player.angle` back, zeros `thrust_key_down` / `move_x` / `move_y` / `is_thrusting`, then ticks the timer down. Field initialised in `reset_player()` and `game_init()`. Makefile SRC updated. Clean build, 332,685-byte exe (+5,495 bytes), no new warnings. | agent/claude | 2026-05-26 |
| Claude | Scheduled-task housekeeping: stashed pre-existing local truncated `src/game.c` in `fuligin/` main worktree (still at commit 8b09a7b — 5751-line broken state from v3/v4 sessions). Fast-forwarded `main` to `fuligin_new/main` (009a3e4) — adopts agent/claude's restored 7894-line `src/game.c`, the EMP Sentinel module, the Solar Flare + Star-Shadow code, and the Five-Zone scaffolding. Verified clean build via `make`: all 16 .c files (now 17 with this session's add) compile and link to `launch_fuligin.exe`. Confirms the v3/v4 work is real and runnable, not just committed in name. | fast-forward | 2026-05-26 |
| Claude | Scavenger Probes (Item 24) integrated: new module `src/enemy_scavenger.[ch]` — leprechaun-analog amber bow-tie probes that do not shoot, only siphon Void Steel. Wired into `game.c`: `#include "enemy_scavenger.h"` after `enemy_emp_sentinel.h`; `scavenger_init()` after `emp_sentinel_init()` in `game_init()`; new collision section 5f (placed between 5e EMP and 5b Rust-Weaver) handles player-bolt vs probe (single-shot kill, +120 score, +4 XP orb, "SCAVENGER DOWN" amber float, refund stolen cargo straight back into `res_void_steel` at probe's last position + cinnabar particle burst + "+VOID STEEL" cyan float + Cugel-9 quip); `scavenger_update(dt, player.pos.x, player.pos.y, res_void_steel)` after `emp_sentinel_update()` in the tick — the module returns siphoned-this-frame count, game.c subtracts from `res_void_steel` and spawns `SFX_EXPLOSION_SM` + cinnabar "-VOID STEEL" float for player feedback; `scavenger_render(g_renderer, camera_pos.x, camera_pos.y, player.pos.x, player.pos.y)` after `emp_sentinel_render()` in `render_entities()`. State machine SEEK → TRACTOR → ESCAPE → WARP with tuning: detect 1100u, tractor lock 220u (break 320u), siphon every 0.45s, cargo cap 3 units, max speed 260 u/s, warp distance 1500u, warp animation 0.6s, pool cap 4. Spawn pacing in `update_spawning`: Zone 1+ AND `res_void_steel > 0` only, 60-90s timer with jitter, 900-1200u spawn ring, Cugel-9 foreshadow quip on arrival. Visual: HUD_AMBER bow-tie (two triangles sharing apex) with HUD_CINNABAR centre dot, brightens while siphoning; HUD_TEXT_CYAN dashed 8-segment tractor beam with sliding pulse phase; shrinking amber ring during WARP. Makefile SRC appended `src/enemy_scavenger.c` after `src/enemy_emp_sentinel.c`. Clean build, 338,635-byte exe (+5,950 bytes), no new warnings (only the two pre-existing ones at game.c:1514 sign-compare and game.c:1631 strncpy truncation). | agent/claude | 2026-05-26 |
| Claude | Sensor Static (Blindness) status malfunction (Item 35, todo.md §6): new `player.sensor_static_timer` float field on `ShipEntity` (mirrored in `include/entities.h` and the inline `src/game.c` duplicate); initialised in `reset_player()` and `game_init()`; ticked down in `update_player_physics()` after the EMP-lock tick (input is NOT suppressed — only HUD sensors blank). Trigger lives inside the Solar Flare ACTIVE branch in `update_progression()`: file-static `sensor_static_roll_t` rolls a 45 % chance every 3.5–6.5 s of jittered interval to set the timer to 2.4 s **only when `solar_flare_in_shadow == 0`** — Star-Shadow cover (Item 34) now blocks the scramble in addition to dampening drain. While shadowed the roll timer is held at ≥ 1 s to prevent an instant-fire on shelter exit. On scramble entry: `SENSOR STATIC` cinnabar event float + `SFX_EXPLOSION_SM` + `cugel9_say("SENSORS BLINDED BY STELLAR WIND. NAVIGATE BY INTUITION OR DESPAIR.")`. `render_minimap()` short-circuits while the timer is positive: panel frame still draws, but interior is replaced with ~1.8 %-density cyan/cinnabar pixel noise (re-seeded every frame for shimmer) + a pulsed `[NO SIGNAL]` cinnabar label. Proximity `>>> DANGER <<<` readout suppressed via same gate. Constants: `SENSOR_STATIC_DURATION = 2.4f`, `SENSOR_STATIC_ROLL_MIN/MAX = 3.5/6.5 s`, `SENSOR_STATIC_HIT_CHANCE = 0.45f`. Clean build, 340,100-byte exe (+1,465 bytes), no new warnings beyond the four pre-existing ones. | agent/claude | 2026-05-26 |

| Claude | Reverse Drift (Confusion) status malfunction (Item 36, todo.md §6): new `player.reverse_drift_timer` float field on `ShipEntity` (mirrored in `include/entities.h` and the inline `src/game.c` duplicate); initialised in `reset_player()` and `game_init()`; ticked down in `update_player_physics()` after the Sensor Static tick (input is NOT suppressed — only thrust direction is flipped). Effect lives in the thrust force block: `float drift_sign = (player.reverse_drift_timer > 0.0f) ? -1.0f : 1.0f;` multiplies both `sinf(thrust_angle)` and `cosf(thrust_angle)` before the velocity accumulation, so engaging thrust pushes the ship **opposite** its heading while active. Trigger lives inside the Solar Flare ACTIVE branch in `update_progression()` directly after the Sensor Static roll: file-static `reverse_drift_roll_t` rolls a 30 % chance every 4.5–8.5 s of jittered interval to set the timer to 3.2 s **only when `solar_flare_in_shadow == 0`** — Star-Shadow cover (Item 34) now blocks the inversion in addition to dampening drain and blocking sensor static. Third reward layer from shielding. While shadowed the roll timer is held at ≥ 1 s to prevent instant-fire on shelter exit. On inversion entry: `REVERSE DRIFT` cinnabar event float + `SFX_EXPLOSION_SM` + `cugel9_say("THRUST POLARITY INVERTED. ENJOY YOUR INVOLUNTARY RETROGRADE.")`. Tuning rationale: 3.2 s duration is longer than Sensor Static's 2.4 s because inverted thrust is more disruptive; 30 % hit chance is lower than Sensor Static's 45 % so the two malfunctions don't compound. Both `sensor_static_roll_t` and `reverse_drift_roll_t` reset to 0 on ACTIVE→IDLE transition so each fresh flare cycle starts clean. Constants: `REVERSE_DRIFT_DURATION = 3.2f`, `REVERSE_DRIFT_ROLL_MIN/MAX = 4.5/8.5 s`, `REVERSE_DRIFT_HIT_CHANCE = 0.30f`. Clean build, 341,163-byte exe (+1,063 bytes), no new warnings beyond the five pre-existing ones (sign-compare, two unused-function, one unused-variable, strncpy-truncation). | agent/claude | 2026-05-26 |
| Claude | Tox-Gas Hallucinations status malfunction (Item 37, todo.md §6, completes the trio): new `player.tox_hallucination_timer` float field on `ShipEntity` (mirrored in `include/entities.h` and the inline `src/game.c` duplicate); initialised in `reset_player()` and `game_init()`; ticked down in `update_player_physics()` after the Reverse Drift tick (input is NOT suppressed — gameplay is untouched). Effect is purely visual in `render_entities()` asteroid block: per-asteroid render-time jitter (±4.5 u position, ±0.22 rad angle, ±6 % scale) keyed off `game_time` and asteroid index so the field shimmers asynchronously; on deterministic-1/3 asteroids (`i % 3 == 0`) a low-alpha cyan triangular ghost-Cacogen silhouette is overlaid at ~85 % radius so simple rocks look like elite UFOs at a glance, alpha pulse-modulated by `halluc_f = timer / DURATION`. Stress-cracking still reads against the unjittered angle (stable cue). Trigger inside Solar Flare ACTIVE in `update_progression()` after the Reverse Drift roll: file-static `tox_hallucination_roll_t` rolls a 25 % chance every 5.0–9.0 s of jittered interval to set the timer to 3.0 s **only when `solar_flare_in_shadow == 0`** — Star-Shadow cover (Item 34) now blocks all three malfunctions, fourth reward layer. `TOX HALLUCINATION` cinnabar event float + `SFX_EXPLOSION_SM` + `cugel9_say("REACTOR FUMES DETECTED. THAT ASTEROID IS PROBABLY NOT A CACOGEN. PROBABLY.")` on entry. Roll timer resets on ACTIVE→IDLE alongside the others. Tuning: 25 % hit chance is the lowest of the trio because visual confusion compounds; expected ~1 hallucination per flare. Constants: `TOX_HALLUCINATION_DURATION = 3.0f`, `TOX_HALLUCINATION_ROLL_MIN/MAX = 5.0/9.0 s`, `TOX_HALLUCINATION_HIT_CHANCE = 0.25f`. Clean build, 342,230-byte exe (+1,067 bytes), no new warnings beyond the five pre-existing ones. | agent/claude | 2026-05-26 |

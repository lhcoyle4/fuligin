# FULIGIN — Action List

## Style Authority Files
Every UI implementation must reference these two sources before touching a pixel:
- **`vibe_presentation.html`** / **`FULIGIN_Guide.pdf`** — canonical visual mockups. Pages 2 and 5 of the PDF show the in-game Qud/Noctis style; page 4 shows the title screen.
- **`FULIGIN_HUD_DESIGN_BRIEF.md`** — detailed spec for FF7R panel geometry, bar sizing, glow values, and color hex codes.

## The Two-Style Rule

**In-game HUD, inventory, item cards, tooltips, Cugel-9 logs, resource readouts** → **Caves of Qud / Noctis terminal aesthetic**: monospace (`Share Tech Mono`), bracket-corner rectangular panels, `[LABEL: VALUE]` notation, acid green data values, cyan structure lines, magenta category tags, all-caps.

**Main menu, pause menu, settings, reliquary selection, shop, warp screen** → **FF7 Remake menu aesthetic**: angled parallelogram panels with ~20° diagonal corner cuts, thin 1px URTH CYAN glowing borders, segmented gauge bars, chevron `>` active-state markers, dark near-black glassmorphism fill.

---

## 🔴 Core Systems

**1. dcimgui integration** ✅ [DONE — panel primitives shipped] — Replace all SDL2 hand-drawn UI with dcimgui. Implement two panel primitives as the foundation for everything else:
- `ui_panel_terminal()` ✅ — Qud/Noctis style: rectangular, bracket-style corner marks (`┌`, `┐`, `└`, `┘` drawn as 8px L-shapes), `#88ccff` 1px border, `#050505` fill, `Share Tech Mono` font. Used for all in-game HUD elements, inventory, tooltips, Cugel-9 log. Visual reference: `FULIGIN_Guide.pdf` p.5 inventory grid.
- `ui_panel_menu()` ✅ — Unified terminal style: wraps `ui_panel_terminal()` + centered `──── TITLE ────` divider. Used for all menu/settings/shop screens. (FF7R angled-cut aesthetic retired in favour of unified Qud terminal look.)

**2. HUD panel geometry** ✅ [DONE] *(terminal style — Qud/Noctis)* — Score, fuel, ammo, chronicle bar, reliquary count, zone indicator all use `ui_panel_terminal()`. No diagonal cuts on combat HUD. Monospace labels in `#88ccff` cyan; data values in `#39FF14` acid green; category/type tags in `#FF00FF` magenta using `[BRACKET]` notation. Reference: `FULIGIN_Guide.pdf` p.2 item cards and p.5 inventory UI.

**3. Segmented / block progress bars** ✅ [DONE] *(terminal style)* — `ui_bar_segmented()` for Chronicle/XP: acid green fill, discrete block segments per 10%, dark `#001100` track, 1px tight border. `ui_bar_block()` for hull/shields. Danger ramp on fuel/hull: `#39FF14` → `#FF8C00` at 30% → `#E34234` at 15%. Label format: `FUEL: 847 / 1200` (monospace, label dim, value bright). Reference: `FULIGIN_HUD_DESIGN_BRIEF.md` §1B Qud bar spec.

---

## 🟠 Visual Polish

**4. Phosphor decay / Vectrex glow** ✅ [DONE] — All vector lines (ships, bullets, UI borders) leave ~0.1s luminance trail via additive blend. Implemented via `vg_apply_persistence()` in `vector_graphics.c` — offscreen render target with exponential decay; 5-level `settings_glow` dial; called every frame at the top of `game_render()`.

**5. Scanline shimmer** ✅ [DONE] — 1px horizontal sweep, 5–10% opacity, every 3–5s. Toggleable via `graphics.scanlines`. Applies only to gameplay viewport, not menu overlays.

**6. Screen-edge vignette** ✅ [DONE] — PHOTIC RUST `#B22222` tint at screen edges, scales with zone depth. CINNABAR pulse on beat peak.

**7. Score / event floats** *(terminal style)* ✅ [DONE] — `Share Tech Mono`, all-caps, scale-up-and-fade over 1.5s. URTH CYAN for Chronicle gain, CINNABAR for damage, ACID GREEN for resource pickup. Format matches item card stat style from `FULIGIN_Guide.pdf` p.2.

**8. Zone transition banner** *(terminal style)* ✅ [DONE] — `>>> ENTERING IRON SHOALS <<<` slides in at top, monospace, GHOST WHITE with full URTH CYAN glow. Bracket-corner framing. Reference: todo.md zone transition spec.

**9. Vector stress-cracking** ✅ [DONE] — High-value asteroids display spreading wireframe fractures before shattering. URTH CYAN cracks, CINNABAR flash at break point.

**10. Warp-drive singularity exit effects** ✅ [DONE] — Three unlockable warp FX: vector expansion (default), black-hole collapse (all screen lines converge to 1px then explode), CRT power-down flash. Progression rewards. *(Default vector-expansion + CRT-bloom-flash variant shipped — fires on every warp jump; black-hole-collapse variant and the unlock-progression system deferred.)*

**11. Engine dust trail** ✅ [DONE] — Phos-green (`#39FF14`) vector particle trail proportional to ship velocity. Ambient cosmic dust sucked in at high speeds.

**12. Glitch tearing on crits** ✅ [DONE] — Crit hit or large rift: vector lines duplicate, shake, tear across horizontal scanlines + white-noise screech.

---

## 🟡 Gameplay Mechanics

**13. Zone transition system** ✅ [PARTIAL — five-zone scaffolding done] — Five zones now indexed 0-4. `get_zone()` returns 0-4 using new `ZONE_ABYSS_RADIUS` (14000u) as the boundary between THE ABYSS (zone 3) and DEEP DRIFT (zone 4). All zone-indexed arrays (`tl_zone_names`, `br_zone_names`, `mm_zn`, `zone_banner_names` in game.c + the 3 mirrors in ui_hud.c) extended to 5 entries with `"DEEP DRIFT"`. `HUD_ZONE_ACCENT[]` and `UI_ZONE_COLORS[]` extended to 5 entries; DEEP DRIFT accent is a bleached bone-white (220,215,190) reading as "light has drained out". `ui_zone_color()` clamp raised to 4. Beat-pulse vignette divisor updated 3.0f → 4.0f so the 0→1 ramp covers the new range. **Side effect: Item 22 Lictors (gated on `player_zone >= 4`) now actually spawn — previously dead-code path.** Warp loci as fixed nav beacons + zone-entry banner integration (item 8 already done) deferred; HUD_DESIGN_BRIEF.md sample array sizes also still say [4] but are doc-only. Lore-name alignment with action-list canonical names (HOME STATION / SCRAP FIELDS / IRON SHOALS / VOID REACHES / DEEP DRIFT) deferred — current code retains HOME SPACE / INNER BELT / DEEP VOID / THE ABYSS / DEEP DRIFT for backward compatibility with shipped saves and lore docs.

**14. Proximity danger alert** *(terminal style)* ✅ [DONE] — Pulsing `>>> DANGER <<<` at bottom-center, CINNABAR, monospace. Matches the terminal bracket-flash style from `vibe_presentation.html`.

**15. Remnant debris / hidden hazards** — Invisible spatial hazards only revealed with PALE SIGHT Reliquary. No visual cue until equipped.

**16. Fuel clock / reactor drain** ✅ [DONE] — Passive reactor drain (0.08 fuel/s base + 0.04/s per active relic) depletes fuel even without thrusting. At 0% fuel: thrust blocked (Emergency Drift Mode), drift_penalty_timer accumulates; after 10 s adrift the ship takes a hull breach (lose a life, fuel refills to 20% emergency reserve, HULL BREACH EventFloat + screen shake). Fuel UI already uses danger ramp via ui_fuel_color().

**17. Emergency heat vent** ✅ [DONE] — Weapon overheat on rapid fire. Player-triggered coolant blowback: dense forward vector particles + instant reverse thrust, consumes fuel.

**18. Gravity slingshot boosts** ✅ [DONE] — Close orbit (150–380 u) around the void rift gives a continuous tangential velocity boost scaled by orbital depth and tangential speed. Fires only when the player is genuinely curving around the rift (|tan_v| > 30). Spawns a 3 s-cooldown "GRAVITY ASSIST" EventFloat + cyan particles on first entry.

**19. Asteroid bowling combos** ✅ [DONE] — Heavy projectile launches chain into larger rocks. `STRIKE!` float in ACID GREEN + multiplier.

**20. Cargo bay / inventory weight** *(terminal style — Qud grid)* — Grid-based cargo slots using the exact inventory grid from `FULIGIN_Guide.pdf` p.5: cyan bracket-corner slot cells, item name in cyan, category tag in magenta `[TYPE]`, stats in acid green. Heavy cargo degrades ship physics.

---

## 🟢 Enemy AI

**21. Ascians** ✅ [DONE] — Voiceless interceptor saucers patrolling regular-polygon perimeters at constant angular speed in Zone 3+. Module `src/enemy_ascian.[ch]` wired into `game.c`: `ascian_init()` in `game_init()`, `ascian_update()` after `rustweaver_update()` in the simulation tick, `ascian_render(g_renderer, camera_pos.x, camera_pos.y)` after the rust-weaver render. Spawn pacing: Zone 3+ only, 55-85s timer, 800-1100u spawn ring, cycles triangle/square/pentagon patrol shapes per spawn. Three-bolt magenta wedge volley when player crosses 600u detect radius (4.2s cooldown). Collision section 5c runs `ascian_hit_test` against player bolts (+150 score, +1 XP orb, ASCIAN DOWN float) and `ascian_check_player_hit` against the player hull (standard projectile damage — Void Stone armour soaks one hit; "INTERCEPTED" float + sad Cugel-9 quip). Voiceless per design contract — module emits no SFX of its own. Clean build, exe produced (319KB), no new warnings.

**22. Lictors** ✅ [DONE] — Elite, aggressive interceptor saucers that pursue the Autodyne at high speeds, Zone 4+ only. Module `src/enemy_lictor.[ch]` wired into `game.c`: `lictor_init()` in `game_init()`, `lictor_update()` after `ascian_update()` in the simulation tick, `lictor_render(g_renderer, camera_pos.x, camera_pos.y)` after `ascian_render()`. Spawn pacing: Zone 4+ only, 90-130s timer, 900-1200u spawn ring; foreshadow Cugel-9 quip on arrival. Pursuit AI: per-frame thrust vector toward the player (240 u/s²) with capped 260 u/s max speed and 0.18/s linear drag — smooth predator arc, not a perfect tracker. Aimed bolt every 2.4 s when player is within 900u detect radius (340 u/s bolt speed, amber streak). Collision section 5d handles player-bolt vs Lictor (+200 score, +1 XP orb, LICTOR DOWN float) and Lictor-bolt vs player (standard hit, Void Stone soak, PURSUED float + Cugel-9 quip). "Arrival accelerates beat" implemented via `lictor_alive_count()` accessor: the global beat-pulse code compresses tempo by ~35 % (clamped at 0.15 s minimum) while any Lictor is alive. Voiceless module — caller plays SFX on hit. Makefile SRC updated. Clean build, 325KB exe, zero new warnings.

**23. Rust-Weaver Drones** ✅ [DONE] — Corrosive spit bypasses shields, degrades hull plating. Module `src/enemy_rustweaver.[ch]` wired into `game.c`: `rustweaver_init()` in `game_init()`, `rustweaver_update()` after `drone_chatter_update()` in the simulation tick, `rustweaver_render(g_renderer, camera_pos.x, camera_pos.y)` after NPC shapes in `render_entities()`. Spawn pacing: Zone 2+ only, 35-55s timer with jitter, 700-1000u spawn ring. Collision section 5b runs `rustweaver_hit_test` against player bolts (+75 score, +1 XP orb, RUSTWEAVER DOWN float) and `rustweaver_check_player_hit` against the player hull (corrosion bypasses Ether Shroud and Phase Shift — direct hull-breach unless Void Stone armour soaks; "CORROSION" float + sad Cugel-9 quip). Makefile SRC line also restored to include the previously-dropped `src/state.c` and `src/game_data.c` so the build links again.

**24. Scavenger Probes** ✅ [DONE] — Tractor-beam Void Steel, attempt warp escape. New module `src/enemy_scavenger.[ch]` wired into `game.c`: `scavenger_init()` after `emp_sentinel_init()` in `game_init()`; `scavenger_update(dt, player.pos.x, player.pos.y, res_void_steel)` after `emp_sentinel_update()` in the simulation tick (return value subtracted from `res_void_steel` so game.c remains the source of truth for the inventory counter); `scavenger_render(g_renderer, camera_pos.x, camera_pos.y, player.pos.x, player.pos.y)` after `emp_sentinel_render()` in `render_entities()`. State machine SEEK → TRACTOR → ESCAPE → WARP with tuning: detect radius 1100u, tractor lock at 220u (break at 320u), siphon interval 0.45s, cargo cap 3 units, max speed 260 u/s, warp distance 1500u, warp animation 0.6s. Visual: amber bow-tie silhouette (two triangles sharing apex) with cinnabar centre dot; dashed cyan beam between probe and player during TRACTOR (8 segments with sliding bright/dim pulse so it reads as "active leech"); brightens to near-cinnabar body while siphoning; shrinking amber ring during WARP. Spawn pacing in `update_spawning`: Zone 1+ only AND `res_void_steel > 0` (no point spawning if there's nothing to steal); 60-90s timer with jitter, 900-1200u spawn ring; pool cap 4; spawn fires a Cugel-9 foreshadow quip (`"UNAUTHORIZED SALVAGE BIDDER APPROACHING. RECOMMEND PRECISION DISCOURAGEMENT."`). Collision section 5f (placed between 5e EMP and 5b Rust-Weaver in the existing comment hierarchy): player-bolt vs probe = single-shot kill, +120 score, +4 XP orb, "SCAVENGER DOWN" amber float; on kill any stolen cargo is refunded directly to `res_void_steel` at the probe's last position with a cinnabar particle burst, "+VOID STEEL" cyan float, and a Cugel-9 quip (`"CARGO RECOVERED. THE UNIVERSE STILL HATES YOU, BUT LESS."`). Siphon tick in update path also plays `SFX_EXPLOSION_SM` + cinnabar "-VOID STEEL" float on the player so the bleed reads in real time. Module is silent — game.c plays all SFX. Probes do not shoot, so there is no projectile-vs-player branch — the threat is entirely loss-of-resource. Makefile SRC updated (`src/enemy_scavenger.c` appended after `src/enemy_emp_sentinel.c`). Clean build, 338,635-byte exe (+5,950 bytes), no new warnings beyond the two pre-existing ones (sign-compare at game.c:1514 and strncpy truncation at game.c:1631).

**25. EMP Sentinels** ✅ [DONE] — Floating-Eye analog disruption unit, Zone 2+. Module `src/enemy_emp_sentinel.[ch]` wired into `game.c`: `emp_sentinel_init()` in `game_init()`, `emp_sentinel_update()` after `lictor_update()` in the simulation tick, `emp_sentinel_render(g_renderer, camera_pos.x, camera_pos.y)` after `lictor_render()` in `render_entities()`. State machine IDLE→CHARGE (1.4 s telegraph)→PULSE (0.45 s, ring expands to 380 u)→COOLDOWN (4 s). Detect radius 500 u. Hex purple body (HUD_PURPLE) with bright cyan pupil; pulse ring is a 16-segment vector circle so it reads vector-arcade rather than raster. Spawn pacing: Zone 2+ only, 50–80 s timer with jitter, 750–1000 u spawn ring; pool cap 3 simultaneous (file-static inside the module). Collision section 5e handles (a) player bolt vs sentinel — single-bullet kill, +100 score, +6 XP orb, `SENTINEL DOWN` cyan float, HUD_PURPLE explosion particles + SFX_EXPLOSION_MD and (b) pulse ring vs player — sets `player.emp_lock_timer = 1.6 f` (re-armed every frame the player remains inside the ring), `EMP LOCK` HUD_PURPLE float + `cugel9_say("STEERING SYSTEMS OFFLINE. CONGRATULATIONS, YOU ARE DEBRIS.")`. While `emp_lock_timer > 0` the input phase in `update_player_physics()` snaps `player.angle` back to its pre-input value and zeros `thrust_key_down` / `move_x` / `move_y` / `is_thrusting` — the player can still fire, but cannot steer or accelerate. Bypasses Ether Shroud and Phase Shift (status effect, not projectile); Void Stone armour does NOT soak it (matches "EM field, not impact" framing). New field `player.emp_lock_timer` added to both `include/entities.h` ShipEntity AND the inline ShipEntity struct in `src/game.c` (the inline duplicate is what game.c actually compiles against — known refactor wart). Field initialised in `reset_player()` and `game_init()`. Makefile SRC updated. Clean build, 332,685-byte exe, no new warnings beyond pre-existing ones.

---

## 🔵 Lore / UI Features

**26. Cugel-9 board computer** ✅ [DONE] *(terminal style)* — HUD text log panel using `ui_panel_terminal()`. Format: `[CUGEL-9]: STRUCTURAL INTEGRITY COMPROMISED. COMPENSATING FOR PILOT INEPTITUDE.` — monospace, dim cyan label, ghost white body text. Plus TTS using sad/melancholic robot voice. Reference: `FULIGIN_Guide.pdf` p.2 lore text style (italic monospace blockquote).

**27. Pet shield drone chatter** ✅ [DONE] *(terminal style)* — Tiny `Share Tech Mono` text floats above orbital drones: `[HELP]`, `"TARGET: BIG ROCK"`, `"SHIELD AT 24%"`. Low-contrast cyan. Integrated into `src/game.c` (NPC update loop emits TARGET_UFO / TARGET_LARGE / HELP / COORDINATING events based on game state with 1.2–6 s re-arm; rendered after NPC shapes in world-camera space; `drone_chatter.c` added to Makefile SRC).

**28. The Rusty Flagon Tavern Beacon** *(menu style)* — Rare neutral station. Docking opens an FF7R-style menu overlay (angled panels) for trading contraband, buying coordinates, or playing vector roulette.

---

## ⚪ Settings / Infrastructure

**29. Settings menu** *(FF7R structure + Caves of Qud visual style)* ✅ [RETROFIT DONE] — Full-screen overlay with FF7R-style tab-based navigation and interactability: tab bar across top, `>` chevron row cursor, keyboard/controller nav between rows, segmented sliders for numeric values, toggle switches for booleans. Visual rendering uses the terminal/Qud aesthetic throughout: `ui_panel_terminal()` bracket-corner panels, `Share Tech Mono` monospace font, `#88ccff` cyan label text, `#39FF14` acid-green current values, `#FF00FF` magenta `[CATEGORY]` section headers, `#001100` dark track on slider fills. No diagonal panel cuts — rectangular terminal panels only. Reference: `FULIGIN_Settings_Spec.md` for full field list; `FULIGIN_HUD_DESIGN_BRIEF.md` §1B for bar/slider spec. **Retrofit complete: all menus, overlays (title screen, pause menu, upgrade selection, high score screen, reliquary shop, gameover, keybinds, warp menu, shop overlay, minimap panel, god-mode banner) now use `ui_panel_terminal()`. The FF7R angled-panel aesthetic is retired. Settings menu interactive implementation (tab nav, sliders, toggles) still pending.**

**30. Consolidate Settings struct** — Reconcile `GameSettings` (spec) with current `Settings` in `game.h`. One canonical struct, one `.cfg` file.

**31. Dynamic CRT glass curvature** [IN PROGRESS] — `render_crt_glass()` static function added to `game.c` (14 edge-darkening strips + 40 corner glare diagonals via plain SDL2 draw calls); `settings_crt_curve` toggle wired in; function called before `vg_present()`. Shader/GLSL barrel distortion deferred. Off by default.

**32. Chronicle chord harmonics** ✅ [DONE] — Rapid orb pickup → sequential FM-synth pentatonic notes. Combo chain = arpeggio.

**33. Solar Flare Cycles** ✅ [DONE] — todo.md §2.3 (Reactor Core & Fuel Clock). Three-phase state machine in `update_progression()`: COUNTUP (90–150s timer, only ticks when `player_zone >= 1`) → WARNING (3s telegraph: `cugel9_say("SOLAR FLARE INBOUND. SEEK ASTEROID SHADOW. SURVIVAL OPTIONAL.")` + amber `FLARE INBOUND` event float) → ACTIVE (5s, passive reactor drain × `SOLAR_FLARE_FUEL_MULT = 3.0f` via hook in `update_player_physics()`'s passive-drain block; spawns cinnabar `SOLAR FLARE` float + 0.35s/4.0 screen shake on entry). Re-roll on exit + cyan `FLARE PASSED` float. Constants: `SOLAR_FLARE_MIN_INTERVAL=90s`, `SOLAR_FLARE_MAX_INTERVAL=150s`, `SOLAR_FLARE_WARN_DURATION=3s`, `SOLAR_FLARE_ACTIVE_DURATION=5s`. Statics: `solar_flare_timer`, `solar_flare_warn_t`, `solar_flare_active_t`, `solar_flare_next_iv`, `solar_flare_in_shadow`. Init in `game_init()` rolls first interval so flares don't fire immediately at startup. Home Space (zone 0) exempt — only triggers when `player_zone >= SOLAR_FLARE_MIN_ZONE`. Verified clean build via Makefile from worktree (327KB exe, no new warnings; only pre-existing sign-compare and unused-symbol notices remain).

**35. Sensor Static (Blindness)** ✅ [DONE] — todo.md §6 Status Malfunctions. New `player.sensor_static_timer` field added to `ShipEntity` in **both** `include/entities.h` AND the inline duplicate in `src/game.c` (the inline copy is what `game.c` compiles against — same refactor wart Item 25 documented). Field initialised to 0 in `reset_player()` and `game_init()`; ticked down in `update_player_physics()` immediately after the EMP-lock tick — input is **not** suppressed (the pilot can still fly while blinded; only the HUD's sensor stack goes offline). Trigger logic lives inside the Solar Flare ACTIVE phase in `update_progression()`: a file-static `sensor_static_roll_t` (rolled to a jittered 3.5–6.5 s interval) ticks down each frame and, when it expires, rolls a 45 % `SENSOR_STATIC_HIT_CHANCE` to set `player.sensor_static_timer = SENSOR_STATIC_DURATION = 2.4f`. The roll only runs when `solar_flare_in_shadow == 0` — hiding under an asteroid blocks the scramble outright, giving Star-Shadow shielding (#34) a second-order reward. When shielded, the roll timer is held at ≥ 1 s so the moment the player leaves cover doesn't trigger an instant scramble. On scramble entry: `SENSOR STATIC` cinnabar event float, `audio_play(SFX_EXPLOSION_SM)`, and `cugel9_say("SENSORS BLINDED BY STELLAR WIND. NAVIGATE BY INTUITION OR DESPAIR.")`. While `sensor_static_timer > 0`, `render_minimap()` replaces the panel interior with sparse cyan/cinnabar pixel noise (~1.8 % fill, re-seeded every frame for shimmer) + a pulsed `[NO SIGNAL]` label centred in the panel, then returns before any blip rendering; the proximity `>>> DANGER <<<` readout in the bottom HUD overlay is also gated off (same sensor stack). The roll timer resets to 0 when the ACTIVE phase exits so a fresh flare cycle starts with a clean roll. Constants live in the file-static block alongside the `solar_flare_*` state. Clean build, 340,100-byte exe (+1,465 bytes over the post-Item-24 baseline), no new warnings beyond the four pre-existing ones (sign-compare, two unused-function, strncpy-truncation).

**36. Reverse Drift (Confusion)** ✅ [DONE] — todo.md §6 Status Malfunctions, companion to #35. New `player.reverse_drift_timer` field added to `ShipEntity` in **both** `include/entities.h` AND the inline duplicate in `src/game.c` (same dual-struct wart). Field initialised to 0 in `reset_player()` and `game_init()`; ticked down in `update_player_physics()` immediately after the Sensor Static tick. Input is **not** suppressed — rotation, fire, and even the thrust key itself still work; what changes is the **sign** of the thrust force vector. Effect lives in the thrust force block at line ~3322: a local `float drift_sign = (player.reverse_drift_timer > 0.0f) ? -1.0f : 1.0f;` is computed each thrust frame and used to multiply both `sinf(thrust_angle)` and `cosf(thrust_angle)` before the velocity accumulation. While the timer is positive, engaging thrust pushes the ship **opposite** its heading — the engines fire backward — so the pilot has to invert their muscle memory or simply rotate 180° and re-thrust. Trigger logic lives inside the Solar Flare ACTIVE phase in `update_progression()` immediately after the Sensor Static roll: a file-static `reverse_drift_roll_t` (rolled to a jittered 4.5–8.5 s interval) ticks down each frame and, when it expires, rolls a 30 % `REVERSE_DRIFT_HIT_CHANCE` to set `player.reverse_drift_timer = REVERSE_DRIFT_DURATION = 3.2f`. The roll only runs when `solar_flare_in_shadow == 0` — hiding under an asteroid blocks the inversion outright, **third** reward layer from Star-Shadow shielding (#34) (after reduced fuel drain and sensor-static immunity). When shielded, the roll timer is held at ≥ 1 s so leaving cover doesn't trigger an instant inversion. On inversion entry: `REVERSE DRIFT` cinnabar event float, `audio_play(SFX_EXPLOSION_SM)`, and `cugel9_say("THRUST POLARITY INVERTED. ENJOY YOUR INVOLUNTARY RETROGRADE.")`. The roll timer resets to 0 in the ACTIVE→IDLE transition alongside `sensor_static_roll_t` so a fresh flare cycle starts clean. Tuning: 3.2 s duration is longer than Sensor Static's 2.4 s because inverted thrust is a more disruptive penalty; 30 % hit chance is lower than Sensor Static's 45 % to compensate so the two malfunctions don't compound into total panic. Constants `REVERSE_DRIFT_DURATION = 3.2f`, `REVERSE_DRIFT_ROLL_MIN/MAX = 4.5/8.5 s`, `REVERSE_DRIFT_HIT_CHANCE = 0.30f` live in the file-static block alongside the `sensor_static_*` and `solar_flare_*` state. Clean build, 341,163-byte exe (+1,063 bytes over Sensor Static), no new warnings beyond the five pre-existing ones.

**37. Tox-Gas Hallucinations** ✅ [DONE] — todo.md §6 Status Malfunctions, third member of the Solar-Flare malfunction trio (after #35 Sensor Static and #36 Reverse Drift). New `player.tox_hallucination_timer` field added to `ShipEntity` in **both** `include/entities.h` AND the inline duplicate in `src/game.c` (same dual-struct wart). Field initialised to 0 in `reset_player()` and `game_init()`; ticked down in `update_player_physics()` immediately after the Reverse Drift tick. Input is **not** suppressed — gameplay collision and physics are untouched. The effect is purely visual: in `render_entities()` the asteroid block computes a frame-coherent `halluc_f = timer / DURATION` and, when positive, jitters each asteroid's render transform — position drifts ±4.5 u via `sinf(game_time * 5.3f + i * 0.91f)`/`cosf(phase * 1.21f)`, angle wobbles ±0.22 rad via `sinf(phase * 0.77f)`, and scale breathes ±6 % via `sinf(phase * 1.93f)` — all multiplied by `halluc_f` so the effect fades smoothly with the timer. The unique per-asteroid phase offset means the field shimmers asynchronously rather than rocking in unison. On deterministic-1/3 asteroids (`i % 3 == 0`) a low-alpha cyan triangular ghost-Cacogen silhouette is overlaid at ~85 % radius (a tip-triangle + crossbar wedge), so simple rocks LOOK like elite UFOs at a glance — the same rocks transform across consecutive frames so the misidentification is consistent, but overlay alpha pulses at 6.7 rad/s for a shimmer feel. Stress-cracking still reads against the real (unjittered) angle so the player has at least one stable cue. Trigger logic lives inside the Solar Flare ACTIVE phase in `update_progression()` immediately after the Reverse Drift roll: a file-static `tox_hallucination_roll_t` (jittered 5.0–9.0 s interval) ticks down each frame and, when it expires, rolls a 25 % `TOX_HALLUCINATION_HIT_CHANCE` to set `player.tox_hallucination_timer = TOX_HALLUCINATION_DURATION = 3.0f`. The roll only runs when `solar_flare_in_shadow == 0` — hiding under an asteroid blocks the scramble outright, **completing the Star-Shadow shielding trio** (Item 34 now blocks reduced fuel drain + sensor-static + reverse-drift + hallucinations). When shielded the roll timer is held at ≥ 1 s so leaving cover doesn't trigger an instant scramble. On hallucination entry: `TOX HALLUCINATION` cinnabar event float, `audio_play(SFX_EXPLOSION_SM)`, and `cugel9_say("REACTOR FUMES DETECTED. THAT ASTEROID IS PROBABLY NOT A CACOGEN. PROBABLY.")`. The roll timer resets to 0 in the ACTIVE→IDLE transition alongside `sensor_static_roll_t` and `reverse_drift_roll_t` so a fresh flare cycle starts clean. Tuning rationale: 3.0 s duration is between Sensor Static's 2.4 s and Reverse Drift's 3.2 s; 25 % hit chance is the lowest of the trio because visual confusion compounds with the other two malfunctions — pilot misidentifying rocks AS UFOs while their minimap is dark AND their thrust is inverted is already maximal panic. Average expected: ~1 hallucination per flare. Constants `TOX_HALLUCINATION_DURATION = 3.0f`, `TOX_HALLUCINATION_ROLL_MIN/MAX = 5.0/9.0 s`, `TOX_HALLUCINATION_HIT_CHANCE = 0.25f` live in the file-static block alongside the `sensor_static_*`, `reverse_drift_*`, and `solar_flare_*` state. Clean build, 342,230-byte exe (+1,067 bytes over Reverse Drift), no new warnings beyond the five pre-existing ones (sign-compare, two unused-function, one unused-variable, strncpy-truncation).

**34. Star-Shadow Radiation Shielding** ✅ [DONE] — todo.md Lore §3. Extends Item 33: during an ACTIVE flare, a per-frame asteroid-shadow scan in `update_progression()` walks the asteroid pool and checks whether any large/medium asteroid (size ≥ 2) lies between the player and the dying sun (origin). Algorithm: compute `u = -player_pos / |player_pos|` (unit vector toward origin); for each asteroid `A`, reject if its distance from origin ≥ player's; project `(A - player) · u`; reject if `t < 0` or `t > |player_pos|`; compute perpendicular distance; accept if `perp ≤ SOLAR_FLARE_SHADOW_HALF_W + 0.6 × asteroid.radius`. If any asteroid passes, `solar_flare_in_shadow = 1`. The drain hook in `update_player_physics()` substitutes `SOLAR_FLARE_SHADOW_MULT = 1.4f` for `SOLAR_FLARE_FUEL_MULT = 3.0f` when shielded — taking cover under a big rock more than halves the flare's reactor drain. Visual: `render_entities()` draws a low-alpha cinnabar vector tether from each shielding asteroid's player-facing edge out to the player; alpha = `60 + 140 × centre_factor × pulse(8rad/s)` so the line shimmers during the flare and brightens as the player centres in the corridor. Drawn before asteroids so silhouettes overlay the lines. Constants: `SOLAR_FLARE_SHADOW_MULT = 1.4f`, `SOLAR_FLARE_SHADOW_HALF_W = 80.0f` (world units). Verified clean build, 327KB exe, no new warnings.

**38. Ablative Plating Wear** ✅ [DONE] — todo.md §4 Plating Durability & Calibration (Rust & Enchantment), first sub-bullet. New `int player.plating_wear` field on `ShipEntity` (mirrored in `include/entities.h` AND the inline duplicate in `src/game.c` — same refactor wart all Solar-Flare-trio items documented). Field initialised to 0 in `game_init()` only — NOT in `reset_player()`, because wear is per-run permanent damage that persists across deaths within a run (Rogue's "permanently degrade maximum armor" semantics). Six combat-impact `lives--` sites are now wear-hooked: (1) asteroid collision, (2) UFO body collision, (3) UFO bolt, (4) Ascian bolt (5c branch), (5) Lictor bolt (5d branch), each `+= 1`; (6) Rust-Weaver corrosive spit (5b branch) `+= 2` because the lore explicitly says it "dissolves plating", so corrosion eats it twice as fast. Each hook spawns an amber `PLATING -1` (or `-2`) EventFloat ~8 u above the existing `HIT`/`INTERCEPTED`/`PURSUED`/`CORROSION` cinnabar float, so the player reads two stacked floats per fatal hit (the cinnabar damage label and the amber wear tick). Critically NOT hooked: hyperspace malfunction (line ~1781, self-inflicted not impact) and Emergency Drift Mode hull breach (line ~3422, already a 10-second penalty), so the wear counter only tracks combat damage. New constants `FUEL_PLATING_WEAR_RATE = 0.005f` (extra fuel/sec per wear point) and `PLATING_WEAR_HEAVY_THRESH = 15` (Cugel-9 escalation threshold) defined in the fuel-clock block at top of game.c (lines ~83-87). Drain hook in `update_player_physics()` passive-drain block: `passive_drain = FUEL_PASSIVE_BASE_RATE + relic_count * FUEL_RELIC_RATE + player.plating_wear * FUEL_PLATING_WEAR_RATE`; Solar Flare multiplier still stacks on top so a battered ship in a flare bleeds at `(0.08 + 15*0.005) * 3.0 = 0.465 fuel/sec`, ~6× a fresh hull's idle. At wear == `PLATING_WEAR_HEAVY_THRESH` (15), Cugel-9 fires `"HULL PLATING CRITICAL. REACTOR SEAL IS NOW DECORATIVE."` exactly once (equality check, so it doesn't repeat past the threshold). Rust-Weaver hook uses a `(wear >= THRESH && wear-2 < THRESH)` window so the +2 jump still triggers the quip when it crosses through. Permadeath-feel without lethal escalation — the player can survive any number of hits, but each one makes the run more fragile. Sets up future Calibration Codes (Rogue §4 second sub-bullet) which can reverse-decrement `plating_wear`. Clean build, 343,766-byte exe (+1,536 bytes over Tox-Gas Hallucinations baseline), no new warnings beyond the five pre-existing ones.

**39. Calibration Codes** ✅ [DONE] — todo.md §4 Plating Durability & Calibration (Rust & Enchantment), second sub-bullet. Rogue's Scrolls-of-Enchant-Armor analogue: rare memory-chip drops from killed combat enemies that reverse-decrement `player.plating_wear` (the Item 38 counter). New `CalibrationCodeEntity` typedef mirrored in BOTH `include/entities.h` AND the inline duplicate in `src/game.c` (same refactor wart as every other ShipEntity-adjacent feature). New `#define MAX_CAL_CODES 12` likewise mirrored. Pool reset in `start_new_game()` and (paranoia-init) in `game_init()`. Two new statics: `spawn_cal_code(Vec2 pos)` claims the next free slot and seeds 25-65 u/s outward drift on a random angle, 14 s lifetime (longer than orbs because the drop is rare and players need a chance to chase); `maybe_drop_cal_code(Vec2 pos, float chance)` rolls uniform [0,1) and centralises drop-rate policy at every caller. Six enemy-kill sites wear-hooked with hand-tuned chances reflecting elite-tier and thematic resonance: UFO 10 %/15 % (small/large), Rust-Weaver 12 % (drone that DEGRADES plating drops the code that REPAIRS it — thematic resonance), EMP Sentinel 12 %, Ascian 18 % (Zone 3+ elite), Lictor 22 % (Zone 4+ pursuit elite, hardest to land), Scavenger 8 % (weakest, cargo refund is the primary reward). Per-frame update loop mirrors the orb pattern: tick lifetime + spin accumulator; record trail samples; magnet-attract with `player_upgrades.magnet_radius + 80u` extended radius and 1100 u/s² pull (1500 in god_mode); integrate velocity; cull on `life <= 0` or distance > 1600 u from player. Collection (player radius + 6 u vs 8 u code radius) decrements `player.plating_wear` by 1, spawns amber `+1 CAL` EventFloat + amber 6-particle burst + `SFX_EXPLOSION_SM`, fires `cugel9_say("CALIBRATION CODE COMPILED. PLATING INCREMENTED. ENJOY YOUR DELUSION.")`; if `plating_wear == 0` already, converts to a 50-point morale bonus with cyan `CAL +50` float so the pickup never feels wasted (`cugel9_say("PLATING ALREADY PRISTINE. CODE FILED UNDER MORALE BOOSTER.")`). Render block in `render_entities()` after the orb loop: 9-line amber bracket-corner box (four corner brackets at ±4 u + central horizontal "data bar" at ±2 u) so it reads as a salvaged memory chip rather than a closed orb square. Alpha pulse-modulated by `0.7 + 0.3 * sin(spin * 3)` and end-of-life fade ramp `life / 2`; faint trail drawn via `vg_draw_shape_trail` with 0.25 alpha tail; subtle `1 + 0.08 * sin(spin * 5)` scale breathing; rotation tied to spin so it reads as "thinking" not dead. Clean build verified from the worktree via `mingw32-make`, 346,471-byte exe (+2,705 bytes over Ablative Plating Wear baseline), no new warnings beyond the five pre-existing ones (sign-compare, two unused-function, two unused-variable, strncpy-truncation). Completes the §4 Plating Durability & Calibration sub-section.

**40. Void-Rust Anomalies** ✅ [DONE] — todo.md §4 Plating Durability & Calibration (Rust & Enchantment), third sub-bullet. Completes the §4 sub-section. Drifting radioactive gas pockets in Zone 2+ that, while the player ship is inside, accumulate plating wear and jitter bullet aim — Rogue's "rust trap + confusion field" analogue mapped onto a continuous-hazard region rather than a one-shot event. New `VoidRustCloudEntity` typedef mirrored in BOTH `include/entities.h` AND the inline duplicate in `src/game.c` (same dual-struct wart every ShipEntity-adjacent feature documents). New `#define MAX_VOID_RUST_CLOUDS 4` likewise mirrored — small pool so the world doesn't clutter. New `player.void_rust_damage_t` float field on `ShipEntity` (both header + inline copy), initialised to `VOID_RUST_DAMAGE_INTERVAL = 2.5 f` in `reset_player()` and `game_init()` so the next cloud entry has a fresh damage timer. File-static state: `void_rust_clouds[MAX_VOID_RUST_CLOUDS]` pool, `void_rust_spawn_timer`, `void_rust_next_iv` (jittered 45–80 s interval), `player_in_void_rust` flag recomputed per frame in `update_progression()` and consumed by both the damage-tick path and the bullet-fire jitter hook, `void_rust_in_cloud_prev` (last-frame flag for entry-only quip). `spawn_void_rust_cloud()` claims the next free pool slot and seeds a 120–180 u radius, 15–40 u/s drift on a random angle, 28 s lifetime, placed 800–1400 u from the player on a random bearing — clouds appear as drifting hazards in the player's navigation arc, never spawned on top of the ship. Periodic spawn in `update_progression()` gated on Zone 2+ (Home Space and Inner Belt are clean; the hazard is meant to feel like contamination from deeper rings of the system). Containment check per frame walks the pool and sets `player_in_void_rust` if any active cloud's `radius + player.radius` overlaps; on the entry transition (in-cloud now, out-of-cloud last frame) spawns a sickly yellow-green `VOID RUST` event float `(170, 220, 70)` and fires `cugel9_say("RADIOACTIVE GAS POCKET. AIM DEGRADED. PLATING DISSOLVING. NOMINAL.")` — only once per entry, not every frame. Two effects while inside: (1) **plating wear tick** in `update_player_physics()` after the Tox-Hallucination tick — decrements `player.void_rust_damage_t` while `player_in_void_rust` is true; on rollover `player.plating_wear += 1` (reuses the Item 38 counter, so Item 39 Calibration Codes can reverse this wear too — the three plating mechanics compose cleanly), spawns amber `PLATING -1` float ~30 u above the ship, plays `SFX_EXPLOSION_SM`, and fires `cugel9_say("HULL PLATING CRITICAL. REACTOR SEAL IS NOW DECORATIVE.")` exactly once if wear hits `PLATING_WEAR_HEAVY_THRESH = 15` (same equality check as the Item 38 hooks). Outside any cloud the timer is held at `VOID_RUST_DAMAGE_INTERVAL` by `update_progression()` so re-entry doesn't fire an instant tick. (2) **muzzle jitter** — at both bullet-fire sites in `update_player_physics()` (forward triple-shot AND the rear gun) each bolt independently picks up `±VOID_RUST_ACCURACY_JITTER = 0.12 rad` random offset before the velocity is computed — so the triple-shot spread still reads as three distinct shots (each jittered separately, not as a single rotated wedge) and the rear gun scatters independently of the prow. Render block in `render_entities()` before asteroids so rocks read on top of the haze: three concentric arc rings at 100 % / 78 % / 52 % of cloud radius, each a 14-segment line loop with `±6 u` per-vertex wobble keyed off `cloud.wobble * (1 + ring * 0.2)` so each ring shimmers at a slightly different frequency, giving a churning gas-pocket feel; ring colour `(170, 220, 70)` at 0.55 / 0.42 / 0.30 ring alpha (`Uint8 = 180 * ring_a * env`); plus 6 deterministic "speck" line-segment particles per cloud anchored to `cloud_index * 0.7 + speck_index * 1.27` so individual specks persist frame-to-frame and swirl rather than strobe; alpha envelope `env = fade_in(1.2 s) * fade_out(3.0 s)` so clouds breathe in and dissipate smoothly rather than popping. Constants live near the existing solar-flare block at top of `game.c`: `VOID_RUST_CLOUD_RADIUS_MIN/MAX = 120/180 u`, `VOID_RUST_CLOUD_LIFE = 28 s`, `VOID_RUST_SPAWN_MIN/MAX_IV = 45/80 s`, `VOID_RUST_MIN_ZONE = 2`, `VOID_RUST_DAMAGE_INTERVAL = 2.5 s`, `VOID_RUST_ACCURACY_JITTER = 0.12 rad`, `VOID_RUST_SPAWN_DIST_MIN/MAX = 800/1400 u`. Tuning rationale: 2.5 s damage interval lets a careful pilot graze the edge of a cloud (with a smart trajectory) and exit before any wear accumulates; a panicked pilot who lingers 10 s eats 4 wear points which materially raises their fuel drain and pushes them toward the next Calibration Code drop. 0.12 rad jitter (≈6.9°) is small enough that mid-range shots still mostly connect but large enough that long-range sniping is unreliable — encourages closing distance, but closing distance into the cloud makes it worse, so the player has to either fight from inside or steer around. 4-slot pool cap means at most four clouds can co-exist; combined with 45–80 s spawn interval the steady-state density in Zone 2+ averages roughly 1–2 active clouds in player view at any time. Clean build verified via `mingw32-make` from worktree, 349,244-byte exe (+2,773 bytes over Calibration Codes baseline), no new warnings beyond the five pre-existing ones (sign-compare, two unused-function, one unused-variable, strncpy-truncation).

---

## Suggested Implementation Order

1 (dcimgui + two panel primitives) → 2 → 3 → 29 → 6 → 5 → 8 → 14 → 13 → everything else.

The two panel primitives from item 1 unlock all other UI work — that is the critical path. Nothing else in the list should be started until `ui_panel_terminal()` and `ui_panel_menu()` are solid.
